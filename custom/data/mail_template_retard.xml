<odoo>
    <data noupdate="0">

        <!-- Modèle d'email : Retard de livraison -->
        <record id="mail_template_retard_livraison" model="mail.template">
            <field name="name">Retard de livraison</field>
            <!-- Modèle = commande client -->
            <field name="model_id" ref="sale.model_sale_order"/>

            <!-- Sujet du mail -->
            <field name="subject">
                <![CDATA[
Retard de livraison – Commande {{ object.client_order_ref or object.so_commande_client or object.name }}
                ]]>
            </field>

            <!-- Expéditeur et destinataire -->
            <field name="email_from">
                ${object.company_id.email or 'noreply@yourcompany.com'}
            </field>
            <field name="email_to">
                ${object.partner_id.email or ''}
            </field>

            <!-- Corps du mail (QWeb) -->
            <field name="body_html">
                <![CDATA[
<t t-set="ref_client"
   t-value="object.client_order_ref or object.so_commande_client or object.name"/>
<t t-set="arc" t-value="object.so_arc_ref or object.name"/>

<t t-set="picking"
   t-value="object.picking_ids and object.picking_ids.sorted('scheduled_date')[0] or False"/>

<t t-set="old_date"
   t-value="object.so_date_de_livraison or (picking and picking.scheduled_date and picking.scheduled_date.date()) or False"/>
<t t-set="old_week"
   t-value="old_date and old_date.isocalendar()[1] or 'XX'"/>

<t t-set="new_date" t-value="object.so_retard_nouvelle_date or False"/>
<t t-set="new_week"
   t-value="new_date and new_date.isocalendar()[1] or 'XX'"/>

<t t-set="motif" t-value="object.so_retard_motif or '—'"/>

<p>Madame, Monsieur,</p>

<p>
    Votre commande référence :
    <b><t t-esc="ref_client"/></b><br/>
    Correspondant à notre ARC :
    <b><t t-esc="arc"/></b><br/>
</p>

<p>
    Qui devait être livrée
    <b>semaine <t t-esc="old_week"/></b>
    doit être reportée à la
    <b>semaine <t t-esc="new_week"/></b>.
</p>

<p>
    <b>Motif :</b> <t t-esc="motif"/>
</p>

<p>
    Mr VOLEAU prendra ultérieurement contact avec vous afin de définir
    les modalités de livraison.<br/>
    Nous restons à votre écoute aux coordonnées en signature de ce mail.
</p>

<p>
    Nous sommes désolés de ne pouvoir honorer notre engagement et restons à
    votre disposition pour de plus amples explications.
</p>
                ]]>
            </field>
        </record>

    </data>
</odoo>

<odoo>
  <!-- ════════════════════════════════════════════════════════════════════════
       PIVOT
  ════════════════════════════════════════════════════════════════════════════ -->
  <record id="view_kpi_delivery_pivot" model="ir.ui.view">
    <field name="name">kpi.delivery.billing.pivot</field>
    <field name="model">kpi.delivery.billing</field>
    <field name="arch" type="xml">
      <pivot string="KPI Affaire : Vente / Appro / Stock">
        <field name="sale_order_name" type="row"/>
        <field name="month_date" type="col" interval="month"/>
        <!-- Mesures vente -->
        <field name="amount_invoiced"    type="measure"/>
        <field name="amount_to_invoice"  type="measure"/>
        <!-- Mesures appro -->
        <field name="amount_purchase_total"                   type="measure"/>
        <field name="amount_po_not_delivered_not_invoiced"    type="measure"/>
        <field name="amount_po_delivered_not_invoiced"        type="measure"/>
        <field name="amount_po_delivered_invoiced"            type="measure"/>
        <!-- Mesures stock -->
        <field name="amount_stock_consumed"                   type="measure"/>
        <field name="order_count"        type="measure"/>
      </pivot>
    </field>
  </record>

  <!-- ════════════════════════════════════════════════════════════════════════
       LISTE
  ════════════════════════════════════════════════════════════════════════════ -->
  <record id="view_kpi_delivery_tree" model="ir.ui.view">
    <field name="name">kpi.delivery.billing.tree</field>
    <field name="model">kpi.delivery.billing</field>
    <field name="arch" type="xml">
      <tree create="false" edit="false">
        <field name="sale_order_name"/>
        <field name="scheduled_datetime"/>
        <field name="month_date"/>
        <field name="iso_year"  optional="hide"/>
        <field name="iso_week"  optional="hide"/>
        <field name="invoice_status"/>
        <field name="order_count" optional="hide"/>

        <!-- ── Vente ─────────────────────────── -->
        <field name="amount_invoiced"                 sum="Total" optional="show"/>
        <field name="amount_to_invoice"               sum="Total" optional="show"/>

        <!-- ── Appro ─────────────────────────── -->
        <field name="amount_purchase_total"           sum="Total" optional="show"/>
        <field name="amount_po_not_delivered_not_invoiced" sum="Total" optional="show"/>
        <field name="amount_po_delivered_not_invoiced"     sum="Total" optional="show"/>
        <field name="amount_po_delivered_invoiced"         sum="Total" optional="show"/>

        <!-- ── Stock / MRP ───────────────────── -->
        <field name="amount_stock_consumed"           sum="Total" optional="show"/>
      </tree>
    </field>
  </record>

  <!-- ════════════════════════════════════════════════════════════════════════
       GRAPH
  ════════════════════════════════════════════════════════════════════════════ -->
  <record id="view_kpi_delivery_graph" model="ir.ui.view">
    <field name="name">kpi.delivery.billing.graph</field>
    <field name="model">kpi.delivery.billing</field>
    <field name="arch" type="xml">
      <graph string="Vente / Appro / Stock par mois" type="bar">
        <field name="month_date"          type="row" interval="month"/>
        <field name="amount_invoiced"     type="measure"/>
        <field name="amount_to_invoice"   type="measure"/>
        <field name="amount_purchase_total" type="measure"/>
        <field name="amount_stock_consumed" type="measure"/>
      </graph>
    </field>
  </record>

  <!-- ════════════════════════════════════════════════════════════════════════
       SEARCH
  ════════════════════════════════════════════════════════════════════════════ -->
  <record id="view_kpi_delivery_search" model="ir.ui.view">
    <field name="name">kpi.delivery.billing.search</field>
    <field name="model">kpi.delivery.billing</field>
    <field name="arch" type="xml">
      <search string="Filtrer">
        <field name="sale_order_name" string="Affaire"/>
        <field name="company_id"      string="Société"/>
        <field name="invoice_status"  string="Statut facturation"/>

        <!-- Filtres sur statut vente -->
        <filter string="À facturer"
                name="flt_to_invoice"
                domain="[('invoice_status','=','to invoice')]"/>
        <filter string="Rien à facturer"
                name="flt_nothing"
                domain="[('invoice_status','=','no')]"/>
        <filter string="Tout facturé"
                name="flt_invoiced"
                domain="[('invoice_status','=','invoiced')]"/>
        <filter string="À facturer + Rien à facturer"
                name="flt_to_invoice_or_no"
                domain="[('invoice_status','in',('to invoice','no'))]"/>

        <!-- Filtres appro -->
        <filter string="Appro avec reliquat non facturé"
                name="flt_po_del_not_inv"
                domain="[('amount_po_delivered_not_invoiced','>',0)]"/>
        <filter string="Appro en attente de livraison"
                name="flt_po_not_del"
                domain="[('amount_po_not_delivered_not_invoiced','>',0)]"/>

        <!-- Filtres stock -->
        <filter string="Stock consommé"
                name="flt_stock_consumed"
                domain="[('amount_stock_consumed','>',0)]"/>

        <separator/>
        <group expand="0" string="Regrouper par">
          <filter string="Affaire"             name="grp_so"     context="{'group_by':'sale_order_name'}"/>
          <filter string="Mois"                name="grp_month"  context="{'group_by':'month_date'}"/>
          <filter string="Semaine"             name="grp_week"   context="{'group_by':'iso_week'}"/>
          <filter string="Statut facturation"  name="grp_invst"  context="{'group_by':'invoice_status'}"/>
        </group>
      </search>
    </field>
  </record>

  <!-- ════════════════════════════════════════════════════════════════════════
       ACTION + MENUS
  ════════════════════════════════════════════════════════════════════════════ -->
  <record id="action_kpi_delivery" model="ir.actions.act_window">
    <field name="name">KPI Affaire (Vente / Appro / Stock)</field>
    <field name="res_model">kpi.delivery.billing</field>
    <field name="view_mode">pivot,graph,tree</field>
    <field name="context">{'search_default_flt_to_invoice_or_no': 1}</field>
    <field name="search_view_id" ref="view_kpi_delivery_search"/>
  </record>

  <menuitem id="menu_kpi_root"     name="KPI Livraison"          parent="sale.sale_menu_root"/>
  <menuitem id="menu_kpi_delivery" name="Facturation vs Livraison"
            parent="menu_kpi_root" action="action_kpi_delivery"/>
</odoo>
